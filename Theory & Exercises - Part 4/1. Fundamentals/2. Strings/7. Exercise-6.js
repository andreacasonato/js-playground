// TEMPLATE LITERAL PRACTICE
// Use template literals to create these sentences:
// Name: "Tom", hobby: "reading" → "Tom enjoys reading"
// Item: "pizza", count: "3" → "I have 3 pizzas"

const firstName = "Tom";
let hobby = "reading";
let item = "pizza";
let count = 3;

console.log(`${firstName} enjoys ${hobby}.`);
console.log(`I have ${count} ${item}s.`);
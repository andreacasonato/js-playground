// Array Manipulation
// let numbers = [3, 1, 4, 1, 5, 9];
// Sort the numbers
// Reverse the array
// Extract the middle two numbers
// Combine with another array [2, 6, 5]

let numbers = [3, 1, 4, 1, 5, 9];
let numbers2 = [2, 6, 5];
let numbers3;

console.log(numbers.sort());
console.log(numbers.reverse());
console.log(numbers.slice(2, 4));
console.log(numbers3 = numbers.concat(numbers2));
// Create a Boolean value for an ID variable.
// Using a ternary operator, create a message variable that will check whether their ID is valid and either allow a person into a venue or not.
// Output the response to the console.

let id = true;

let access = id ? console.log('You may enter the venue') : console.log('You\'re not allowed into the venue today');

console.log(access);
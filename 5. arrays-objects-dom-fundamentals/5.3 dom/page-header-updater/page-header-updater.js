// 1. Select the element and store it in a variable.
// This variable now holds a reference to the h1 element itself.
let mainTitle = document.getElementById('main-title');

// 2. Use the variable to change the element's text content.
mainTitle.textContent = "DOM Manipulation is Fun!";

// Use Math.random() to generate a random number between 0 and 1 and log it.
// Use Math.random() and other Math methods to generate a random integer between 1 and 10 (inclusive). Log the result.
// Use Math.sqrt() to find the square root of any positive number of your choice (for example, 25) and log the result.

console.log(Math.random());

console.log(Math.floor(Math.random() * 10) + 1);

console.log(Math.sqrt(9));
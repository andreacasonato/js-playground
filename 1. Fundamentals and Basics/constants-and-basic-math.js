// Declare a constant PI with the value 3.14159.
// Declare a variable radius and assign it a value of 5.
// Calculate the area of a circle (PI * radius²) and the circumference of a circle (2 * PI * radius). Store the results in two new variables and log them to the console.

const PI = 3.14159;

let radius = 5;

let circleArea = PI * radius;
let circleCircumference = 2 * PI * radius;

console.log(circleArea); // 15.70795
console.log(circleCircumference); // 31.4159